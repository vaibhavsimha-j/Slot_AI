name: Keep Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Slot AI
        run: curl -A "Mozilla/5.0" -L "https://slotai.streamlit.app" -o /dev/null -s || true
