name: Keep Alive

on:
  schedule:
    - cron: '*/30 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Slot AI
        run: curl -s -o /dev/null https://slotai.streamlit.app
